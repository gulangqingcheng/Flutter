# Flutter Hello World 个性化示例 — 刘欣雨

本示例用于创新实验第14周课堂任务。

## 运行

```powershell
flutter run
```

## 个性化修改说明

本次在示例基础上完成了以下 **7 处**个性化修改：

| # | 修改位置 | 修改内容 |
|---|---------|---------|
| 1 | 主题色 | 由 `Colors.indigo` 改为 `Colors.deepPurple` |
| 2 | AppBar 标题 | 改为「刘欣雨的创新实验 Flutter 首页」，并居中显示 |
| 3 | 图标 | 由 `rocket_launch` 改为 `school`，颜色改为深紫色，尺寸调整为 80 |
| 4 | 主提示语 | 改为含姓名的个性化文案，字体颜色改为深紫色 |
| 5 | 个人信息栏 | 新增带圆角边框的卡片，展示姓名和课程信息 |
| 6 | 计数含义 | 改为「本次已完成打卡：X 次」，字体颜色同主题色 |
| 7 | 按钮文案与样式 | 图标改为 `done_all`，文案改为「完成一次打卡 ✔」，按钮颜色改为深紫色 |

## 提交记录

```powershell
git add lib/main.dart
git commit -m "personalize flutter hello page"
git push
```

## 课堂提交信息

```
姓名：刘欣雨
我修改了：主题色、AppBar 标题、图标、页面提示语、个人信息卡片、计数含义、按钮文案与颜色
截图：见截图文件
```
